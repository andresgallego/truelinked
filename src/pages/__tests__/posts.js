import React from 'react';
import renderer from 'react-test-renderer';
import Posts from '../posts';

describe('Posts', () => {
  // it('renders correctly', () => {
  //   const tree = renderer
  //     .create(<Posts location={{ pathname: '/posts/2' }} />)
  //     .toJSON();
  //   expect(tree).toMatchSnapshot();
  // });
});
